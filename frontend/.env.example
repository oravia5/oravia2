# Oravia Frontend Environment Template
# Note: Ably is authenticated securely via Backend token endpoint, so frontend key is only required if running standalone serverless chat queries.
VITE_ABLY_API_KEY=your_ably_client_side_key
VITE_API_URL=http://your_ec2_ip_or_domain/api
